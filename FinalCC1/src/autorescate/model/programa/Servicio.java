package autorescate.model.programa;

/**
 * Tipos de servicio que puede prestar AutoRescate 24/7.
 */
public enum Servicio {
    /** Grua para remolque de vehiculos. */
    GRUA,
    /** Moto de apoyo para asistencias rapidas. */
    MOTO_APOYO,
    /** Camioneta equipada para asistencia en ruta. */
    CAMIONETA_ASISTENCIA,
    /** Vehiculo liviano de respuesta rapida. */
    VEHICULO_LIVIANO
}
